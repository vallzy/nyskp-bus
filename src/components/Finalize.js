import { Card } from 'antd';
import React, { useState } from 'react';

const Finalize = (props) => {
    return(
        <>
        <Card bordered={false}>
            <p>The end</p>
        </Card>
        </>
    )
}

export default Finalize;